name: CI/CD Pipeline from Private Repo Trigger

on:
  repository_dispatch:
    types: [trigger-from-private]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Clone Private Repo
        run: |
          git clone https://x-access-token:${{ secrets.PRIVATE_REPO_TOKEN }}@github.com/11vijays/gate-backend.git repo

      - name: Show Repo Structure
        run: |
          ls -la repo
          ls -la repo/app
          ls -la repo/tests

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install Python Dependencies
        run: |
          cd repo
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Pytest
        run: |
          cd repo
          pytest tests

  build:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Clone Private Repo
        run: |
          git clone https://x-access-token:${{ secrets.PRIVATE_REPO_TOKEN }}@github.com/11vijays/gate-backend.git repo

      - name: Docker Login
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      - name: Build & Push Docker Image
        run: |
          cd repo/app
          docker build -t vijaydev11/fastapi-app:latest .
          docker push vijaydev11/fastapi-app:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Clone Private Repo
        run: |
          git clone https://x-access-token:${{ secrets.PRIVATE_REPO_TOKEN }}@github.com/11vijays/gate-backend.git repo

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: gategroup
          slot-name: Production
          images: vijaydev11/fastapi-app:latest
